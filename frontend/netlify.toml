# Netlify config for Ripple DevOps frontend
[build]
  command = "npm run build"
  publish = "dist"

# SPA: serve index.html for all routes
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
